<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Profile Update</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
        background-color: #1a202c; /* Dark background */
        color: #e2e8f0; /* Light text for contrast */
      }
      .card {
        background-color: #2d3748; /* Darker card background */
        border-radius: 1rem;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.2),
          0 4px 6px -2px rgba(0, 0, 0, 0.1);
      }
      .input-field {
        border-color: #4a5568; /* Darker border for inputs */
        background-color: #242b38; /* Darker input background */
        color: #e2e8f0; /* Light text in inputs */
        transition: border-color 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
      }
      .input-field:focus {
        border-color: #6366f1; /* Indigo focus ring */
        outline: none;
        box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.3); /* Indigo focus shadow */
      }
    </style>
  </head>
  <body class="flex items-center justify-center min-h-screen p-4">
    <div class="card p-8 w-full max-w-lg mx-auto">
      <div class="flex flex-col items-center mb-8">
        <div class="mb-4 text-indigo-400">
          <i class="fas fa-user-edit text-5xl"></i>
        </div>
        <h2 class="text-4xl font-extrabold text-white mb-2">Update Profile</h2>
        <p class="text-sm text-gray-400">
          Manage your personal and contact details.
        </p>
      </div>

      <form
        class="space-y-6"
        method="post"
        action="/adminSelfEdit/<%= adminDetails._id %>"
      >
        <div>
          <label
            for="profileImage"
            class="block text-sm font-medium text-gray-300 mb-1"
            >Profile Photo URL</label
          >
          <input
            type="url"
            id="profileImage"
            name="profileImage"
            placeholder="e.g., https://example.com/photo.jpg"
            class="mt-1 block w-full px-4 py-3 border rounded-lg input-field sm:text-base"
            value="<%= adminDetails.profileImage %>"
          />
        </div>

        <div>
          <label
            for="firstName"
            class="block text-sm font-medium text-gray-300 mb-1"
            >First Name</label
          >
          <input
            type="text"
            id="firstName"
            name="firstName"
            required
            maxlength="50"
            placeholder="Enter your first name"
            value="<%= adminDetails.firstName %>"
            class="mt-1 block w-full px-4 py-3 border rounded-lg input-field sm:text-base"
          />
        </div>

        <div>
          <label
            for="lastName"
            class="block text-sm font-medium text-gray-300 mb-1"
            >Last Name</label
          >
          <input
            type="text"
            id="lastName"
            name="lastName"
            required
            maxlength="50"
            placeholder="Enter your last name"
            value="<%= adminDetails.lastName %>"
            class="mt-1 block w-full px-4 py-3 border rounded-lg input-field sm:text-base"
          />
        </div>

        <div>
          <label
            for="email"
            class="block text-sm font-medium text-gray-300 mb-1"
            >Email Address</label
          >
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="you@example.com"
            pattern="^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$"
            title="Please enter a valid email address (e.g., example@domain.com)"
            class="mt-1 block w-full px-4 py-3 border rounded-lg input-field sm:text-base"
            value="<%= adminDetails.email %>"
          />
        </div>

        <div>
          <button
            type="submit"
            class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-150 ease-in-out transform hover:scale-105"
          >
            Save Changes
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
