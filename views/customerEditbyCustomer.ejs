<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
        background-color: #1a202c;
        color: #e2e8f0;
      }
    </style>
  </head>
  <body class="flex items-center justify-center min-h-screen p-4">
    <div
      class="bg-gray-800 p-8 rounded-2xl shadow-2xl border border-gray-700 w-full max-w-md mx-auto"
    >
      <h2 class="text-3xl font-extrabold text-center text-white mb-8">
        Edit Your Profile
      </h2>

      <form
        class="space-y-6"
        method="post"
        action="/customerreviews/customerEditbyCustomer/<%= customerSelfUpdate._id %>"
      >
        <div class="space-y-2">
          <label for="profileImage" class="text-sm font-semibold text-gray-300"
            >Profile Photo URL</label
          >
          <input
            type="text"
            id="profileImage"
            name="profileImage"
            placeholder="e.g., https://example.com/image.jpg"
            value="<%= customerSelfUpdate.profileImage %>"
            class="w-full px-4 py-3 bg-gray-700 text-white rounded-lg border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-150 ease-in-out"
          />
        </div>

        <div class="space-y-2">
          <label for="firstName" class="text-sm font-semibold text-gray-300"
            >First Name</label
          >
          <input
            type="text"
            id="firstName"
            name="firstName"
            required
            maxlength="50"
            placeholder="Your first name"
            value="<%= customerSelfUpdate.firstName %>"
            class="w-full px-4 py-3 bg-gray-700 text-white rounded-lg border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-150 ease-in-out"
          />
        </div>

        <div class="space-y-2">
          <label for="lastName" class="text-sm font-semibold text-gray-300"
            >Last Name</label
          >
          <input
            type="text"
            id="lastName"
            name="lastName"
            required
            maxlength="50"
            placeholder="Your last name"
            value="<%= customerSelfUpdate.lastName %>"
            class="w-full px-4 py-3 bg-gray-700 text-white rounded-lg border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-150 ease-in-out"
          />
        </div>

        <div class="pt-4">
          <button
            type="submit"
            class="w-full py-3 px-4 bg-green-600 text-white font-bold rounded-lg shadow-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 transition-all duration-300 transform hover:scale-105"
          >
            Update My Details
          </button>
        </div>

        <div class="text-center">
          <a
            href="#"
            onclick="history.back()"
            class="text-gray-400 hover:text-white transition-colors duration-200"
            >Cancel</a
          >
        </div>
      </form>
    </div>
  </body>
</html>
